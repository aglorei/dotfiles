---
name: build

on: [push, pull_request]

jobs:
  ansible-lint:
    container:
      image: quay.io/ansible/toolset:0.2
    name: Lint Ansible project
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: ansible-lint --force-color
